[package]
name = "braid-route"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
braid-quote = { path = "../braid-quote" }
# The snapshot a route is planned against, and the plan handed to execution.
serde_json = "1"
ethnum.workspace = true
